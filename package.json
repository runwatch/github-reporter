{
  "name": "github-reporter",
  "version": "1.0.3",
  "description": "GitHub Action to report CI workflow metrics to the RunWatch Pipeline Pulse Portal",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "pnpm run format && pnpm run lint && tsc --noEmit && esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js --format=esm --sourcemap --legal-comments=linked",
    "package": "pnpm run build",
    "lint": "eslint src/**/*.ts",
    "format": "prettier --write \"src/**/*.ts\""
  },
  "keywords": [
    "github-action",
    "ci",
    "metrics",
    "monitoring"
  ],
  "author": "RunWatch",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/github": "^9.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@types/node": "^20.19.37",
    "esbuild": "^0.27.4",
    "eslint": "^9.39.4",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.57.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
